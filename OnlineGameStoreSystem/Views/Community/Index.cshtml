@{
    ViewData["Title"] = "Community";
}

@model CommunityViewModel


<div class="page-container">
    @* 标题 *@
    <h1 class="page-title">Community</h1>

    @* 发布按钮 *@
    <div class="publish-button-wrapper">
        <a asp-controller="Community" asp-action="PostManage" class="button primary-btn">
            Manage Your Post
        </a>
    </div>

    <div class="masonry-container">
        @if (!Model.Posts.Any())
        {
            <p class="no-posts-message">No posts available. Be the first to <a asp-controller="Community" asp-action="PublishPage">write a post</a>!</p>
        }
        else
        {
            @foreach (var post in Model.Posts)
            {
                <a asp-controller="Community" asp-action="PostDetail" asp-route-id="@post.Id" class="masonry-link">
                    <div class="masonry-item">
                        <!-- 封面图 -->
                        <div class="thumbnail-wrapper">
                            @if (!string.IsNullOrEmpty(post.ThumbnailUrl))
                            {
                                <img src="@post.ThumbnailUrl" alt="@post.Title" class="post-thumbnail" />
                            }

                            <!-- 左上角浏览数 -->
                            <div class="view-badge">
                                <i class="fa fa-eye eye-icon"></i>
                                <span>@post.ViewCount</span>
                            </div>
                        </div>

                        <!-- 用户区域 -->
                        <div class="user-info">
                            <div class="avatar-area">
                                <img src="@(string.IsNullOrEmpty(post.AuthorAvatarUrl) ? "/images/avatar_default.png" : post.AuthorAvatarUrl)" class="avatar" />
                            </div>

                            <div class="user-info-right">
                                <div class="username">@post.AuthorName</div>
                                <div class="user-line"></div>
                            </div>
                        </div>

                        <!-- 标题 -->
                        <h3 class="post-title">@post.Title</h3>

                        <!-- 内容摘要 -->
                        <p class="post-snippet">@post.ContentSnippet</p>

                        <!-- 点赞和留言数 -->
                        <div class="post-interactions">
                            <span class="like-icon"><i class="fa fa-heart"></i> @post.LikeCount</span>
                            <span class="comment-icon"><i class="fa fa-comment"></i> @post.CommentCount</span>
                        </div>
                    </div>
                </a>
            }
        }
    </div>
</div>


@section Styles {
    <link rel="stylesheet" href="~/css/pages/community.css" asp-append-version="true" />

}

@section Scripts {
    <script src="~/js/pages/community_mansory.js" asp-append-version="true"></script>

}